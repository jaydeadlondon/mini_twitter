# Social Feed API (Mini-Twitter) 🚀

![Python](https://img.shields.io/badge/Python-3.11-blue?style=flat&logo=python&logoColor=white)
![FastAPI](https://img.shields.io/badge/FastAPI-0.109-009688?style=flat&logo=fastapi&logoColor=white)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-15-336791?style=flat&logo=postgresql&logoColor=white)
![Redis](https://img.shields.io/badge/Redis-7.0-DC382D?style=flat&logo=redis&logoColor=white)

> Современный асинхронный REST API для социальной сети в стиле Twitter. Построен на FastAPI с использованием PostgreSQL и Redis для обеспечения высокой производительности и масштабируемости.

## ✨ Основные возможности

### 🔐 Аутентификация и безопасность
- **JWT токены** для безопасной авторизации
- **Bcrypt** для хеширования паролей
- **Регистрация и вход** пользователей
- **Rate Limiting** через Redis (5 постов/минуту)

### 📰 Лента новостей
- **Умная лента подписок** с оптимизированными SQL-запросами
- **Персонализированный контент** на основе подписок
- **Эффективная пагинация** для больших объемов данных

### 📸 Медиа контент
- **Загрузка изображений** с поддержкой различных форматов
- **Локальное хранилище** медиафайлов
- **Привязка медиа к постам**

### 💬 Взаимодействие
- **Лайки** постов и комментариев
- **Комментарии** с вложенной структурой
- **Система подписок** (follow/unfollow)
- **Просмотр профилей** пользователей

### ⚙️ Администрирование
- **GUI панель управления** (SQLAdmin)
- **Управление пользователями, постами и комментариями**
- **Мониторинг активности**

## 🚀 Быстрый старт

Проект полностью упакован в Docker для простого развертывания.

### 📋 Предварительные требования

- Docker и Docker Compose установлены на вашей системе
- Порт 8000 свободен (или измените в `docker-compose.yml`)

### 🔧 Установка

1. **Создайте файл `.env`** в корне проекта:
   ```ini
   PROJECT_NAME=SocialFeed
   POSTGRES_SERVER=db
   POSTGRES_USER=postgres
   POSTGRES_PASSWORD=postgres
   POSTGRES_DB=social_feed
   POSTGRES_PORT=5432
   SECRET_KEY=your-secret-key-here
   ALGORITHM=HS256
   ```

2. **Запустите контейнеры**:
   ```bash
   docker-compose up -d --build
   ```

3. **Примените миграции базы данных** (выполняется автоматически при первом запуске):
   ```bash
   docker-compose exec app alembic upgrade head
   ```

Готово! 🎉 API доступен по адресу `http://localhost:8000`

## 🌐 Веб-интерфейсы

После успешного запуска доступны следующие веб-интерфейсы:

### 📚 Swagger UI
**URL:** http://localhost:8000/docs

Интерактивная документация API с возможностью:
- Просмотра всех доступных эндпоинтов
- Тестирования запросов прямо в браузере
- Просмотра схем данных и моделей
- Авторизации через JWT токены

### ⚙️ Admin Panel
**URL:** http://localhost:8000/admin

Графический интерфейс для управления:
- 👥 Пользователями и их профилями
- 📝 Постами и контентом
- 💬 Комментариями
- 📸 Медиафайлами

## 🛠 Технологический стек

- **FastAPI** - современный асинхронный веб-фреймворк
- **SQLAlchemy** (async) - ORM для работы с базой данных
- **Alembic** - система миграций базы данных
- **PostgreSQL** - реляционная база данных
- **Redis** - кэширование и rate limiting
- **Pydantic** - валидация данных и схемы
- **SQLAdmin** - административная панель
- **JWT** - токены для аутентификации
- **Bcrypt** - хеширование паролей
- **Docker** - контейнеризация приложения

## 📂 Структура проекта

```text
mini_twitter/
├── docker-compose.yml      # Конфигурация сервисов (App, DB, Redis)
├── Dockerfile              # Сборка образа API
├── requirements.txt        # Python зависимости
├── alembic.ini             # Конфигурация миграций
├── migrations/             # Файлы миграций версий БД
│   └── versions/           # История изменений схемы БД
├── uploaded_files/         # Локальное хранилище медиа (Volume)
└── app/
    ├── main.py             # Точка входа, инициализация FastAPI и Admin
    ├── core/               # Ядро приложения
    │   ├── config.py       # Валидация переменных окружения
    │   ├── database.py     # Настройки Async SQLAlchemy
    │   ├── security.py     # Хеширование паролей и JWT
    │   └── admin.py        # Настройки SQLAdmin (отображение колонок)
    ├── models/             # ORM Модели (User, Post, Comment, Media)
    ├── schemas/            # Pydantic схемы (сериализация данных)
    └── api/
        ├── deps.py         # Зависимости (get_current_user, rate limiting)
        └── endpoints.py    # Все HTTP обработчики (CRUD операции)
```

## 👤 Автор

**jaydeadlondon**

- GitHub: [@jaydeadlondon](https://github.com/jaydeadlondon)

## 📝 Лицензия

MIT License
---
